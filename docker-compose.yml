services:
    postgres:
        image: postgres:latest
        environment:
            POSTGRES_PASSWORD: safepass1
        ports:
            - "5432:5432"
        volumes:
            - pgdata:/var/lib/postgresql/data
volumes:
    pgdata:
